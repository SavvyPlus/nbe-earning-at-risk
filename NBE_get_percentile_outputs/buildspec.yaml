version: 0.2

phases:
  install:
    commands:
      - pip install --upgrade pip
      - pip install -r NBE_process_trade_data/requirements.txt -t NBE_process_trade_data

artifacts:
  base-directory: NBE_process_trade_data
  files:
    - '**/*'
  type: zip

cache:
  paths:
    - /root/.cache/pip
