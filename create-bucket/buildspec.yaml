version: 0.2

phases:
  install:
    commands:
      - pip install --upgrade pip
      - pip install -r create-bucket/requirements.txt -t create-bucket

artifacts:
  base-directory: create-bucket
  files:
    - '**/*'
  type: zip

cache:
  paths:
    - /root/.cache/pip
